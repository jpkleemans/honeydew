/// <reference path="../type_definitions/Jasmine/jasmine.d.ts" />

module Honeydew.Spec
{
    describe("Context", () =>
    {
        var context:Context;

        beforeEach(() =>
        {
            context = new Context();
        });

        it("should have a key", () =>
        {

        });
    });
}
